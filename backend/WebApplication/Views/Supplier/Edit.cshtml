@model WebApplication.Models.Dto.SupplierDto
@{
    ViewData["Title"] = "Edit Supplier";
}

<h2 class="text-2xl font-bold mb-4">Edit Supplier</h2>

<form asp-action="Edit" method="post" class="space-y-4">
    <input type="hidden" asp-for="SupplierId" />

    <div>
        <label asp-for="SupplierName" class="block font-medium">Name</label>
        <input asp-for="SupplierName" class="form-input w-full border rounded px-3 py-2" />
        <span asp-validation-for="SupplierName" class="text-red-500 text-sm"></span>
    </div>

    <div>
        <label asp-for="SupplierEmail" class="block font-medium">Email</label>
        <input asp-for="SupplierEmail" class="form-input w-full border rounded px-3 py-2" />
        <span asp-validation-for="SupplierEmail" class="text-red-500 text-sm"></span>
    </div>

    <div>
        <label asp-for="SupplierPhoneNumber" class="block font-medium">Phone Number</label>
        <input asp-for="SupplierPhoneNumber" class="form-input w-full border rounded px-3 py-2" />
        <span asp-validation-for="SupplierPhoneNumber" class="text-red-500 text-sm"></span>
    </div>

    <button type="submit" class="bg-green-600 text-white px-4 py-2 rounded">Save Changes</button>
    <a asp-action="Index" class="ml-4 text-gray-600 hover:underline">Back to List</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
