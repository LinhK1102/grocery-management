@{
    ViewData["Title"] = "Dashboard";
}

<div class="grid grid-cols-5 sm:grid-cols-6 lg:grid-cols-6 gap-6">
    @await Html.PartialAsync("metric-group/metric-group-01.cshtml")
</div>

<div class="mt-6 grid grid-cols-1 lg:grid-cols-2 gap-6">
    @await Html.PartialAsync("chart/chart-01.cshtml")
    @await Html.PartialAsync("chart/chart-02.cshtml")
</div>
<div class="card bg-base-100 shadow-xl p-4">
    <h2 class="text-2xl font-bold">🌈 Tailwind & DaisyUI Active!</h2>
    <button class="btn btn-primary mt-4">Click Me</button>
</div>

<div class="mt-6">
    @await Html.PartialAsync("table/table-01.cshtml")
</div>

